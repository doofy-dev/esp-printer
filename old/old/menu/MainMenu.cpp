#include "menu/MainMenu.h"

void MainMenu::draw(Display *display) {
    //Draw temp
    //Draw humidity
    //Draw pressure
    //Draw printer info (connected/not connected, print progress, temps)
}

void MainMenu::processInput(Joystick *joystick) {
    MenuPoint::processInput(joystick);
}
